version: 2.1

orbs:
  node: circleci/node@5.0.2
  sonarcloud: sonarsource/sonarcloud@1.1.1
  slack: circleci/slack@4.10.1

executors:
  pnpm-executor:
    docker:
      - image: vernaillen/circleci-pnpm:18.4.0
    working_directory: /tmp/workspace

jobs:
  prepare:
    executor: pnpm-executor
    steps:
      - checkout
      - run: pnpm config set auto-install-peers true
      - run: pnpm install --shamefully-hoist=true
  test-all:
    executor: pnpm-executor
    steps:
      - run: pnpm run test
      - sonarcloud/scan
  plugin:
    executor: pnpm-executor
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run: pnpm run plugin:build
  demo:
    executor: pnpm-executor
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run: pnpm run demo:build
  docs:
    executor: pnpm-executor
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run: pnpm run docs:build
  demo-ssg:
    executor: pnpm-executor
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run: pnpm run demo:build
  deploy:
    executor: pnpm-executor
    steps:
      - slack/notify:
          channel: circleci
          event: always

workflows:
  version: 2
  build_test_all:
    jobs:
      - prepare
      - test-all:
          requires:
            - prepare
          context: 
            - SonarCloud
      - plugin:
          requires:
            - prepare
      - docs:
          requires:
            - prepare
      - demo:
          requires:
            - plugin
      - demo-ssg:
          requires:
            - plugin
      - deploy:
          requires:
            - demo
            - demo-ssg
            - docs
          context: 
            - Slack